# Upgrade Report — 13 July 2026

Codebase: `Sarvadnya` (D:/Yogesh/GitHub/Sarvadnya)
Reviewed by: Claude Code (claude-sonnet-5)
Scope: Full repo review (structure, correctness, redundancy, tests, discoverability, community signals, asset health, attribution/redistribution risk, personal/sensitive info). Follow-up to `reports/upgrade_07042026.md`.

Status: **All items executed** (session of 13 July 2026, phases 1-8). Two items intentionally left, noted inline below.

---

## Checklist

### Structural Changes
- [x] Delete `src/ask_dataframe/.rasa/cache/` (59 tracked files, ~81MB) — ephemeral Rasa NLU build cache with randomly-named tmp dirs, incl. 3 duplicate 27MB TF model checkpoints
- [x] Add `.rasa/` to root `.gitignore` so the cache can't be re-committed
- [x] Delete `src/ask_kautilya/langchain_streamlit_main.py.bak` (stray backup file)
- [x] Delete `src/ask_gst_faq_chatbot/run_server_old.bat` (stray "_old" backup file)
- [x] Decide fate of `src/ask_kautilya/AskKautilya_Nov15.zip` (15MB, unreferenced anywhere) — deleted
- [x] Create `src/utils/README.md` explaining `rag_loader.py`
- [x] Rewrite `src/README.md` catalog: stale entries removed, missing projects added and linked. (Note: `ask_gst_faq_chatbot`, `ask_chintu`, `ask_murphy`, `ask_puzzles`, `ask_shakespere` were deleted per decisions below, so were not added to the catalog.)
- [x] Fix dead link in root `README.md`: `[here](./README_AABB.md)` — link removed (no replacement target existed)

### Critical Issues
- [x] Resolve license conflict — moot: `ask_gst_faq_chatbot` deleted entirely (see Redundancy below)
- [x] Fix bare `except:` clauses: `ask_graph/graphrag_main.py:115`, `ask_manahprarupe/rag_gemma.py:446`, `ask_dataframe/dfengine.py:54`
- [x] Replace `print(e)` with `logging`: `ask_faqs_chatbot/vectorizers/doc2vecgenerator.py:56`, `ask_dataframe/app.py:28`, `ask_dataframe/dfllm.py:47` (4th instance, `ask_gst_faq_chatbot/app.py:38`, moot — file deleted)
- [x] Add `requirements.txt` (pinned) to `ask_dataframe` — done. `ask_gst_faq_chatbot` moot (deleted)
- [x] Decide on attribution/redistribution risk for third-party-only data folders — decision: **delete**. All four had no code at all (data-only folders), so deleting the data removed the folders entirely:
  - `ask_puzzles/` — deleted
  - `ask_murphy/` — deleted
  - `ask_chintu/` — deleted
  - `ask_shakespere/` — deleted

### Redundancy & Asset Bloat
- [x] Decide whether to remove/archive `ask_gst_faq_chatbot` — decision: **removed entirely**
- [x] Re-encode the 10 largest committed videos (~205MB total) smaller in place — done via ffmpeg (libx264, CRF 28, capped 30fps): ~176MB to ~15MB (91% reduction). Working tree only, `.git` history unchanged as noted.
- [x] Add `requirements.txt` to the remaining 14 projects that lack one — 9 added (`ask_career_transition`, `ask_floorplans`, `ask_kautilya`, `ask_manim`, `ask_medicine`, `ask_paulgraham`, `ask_suntzu`, `ask_text2star`, `ask_yhk`); the other 5 (`ask_chintu`, `ask_gst_faq_chatbot`, `ask_murphy`, `ask_puzzles`, `ask_shakespere`) moot — deleted. Note: `ask_paulgraham`, `ask_suntzu`, `ask_yhk` use old (2023-era) LangChain/LlamaIndex APIs — pins are best-effort, flagged in-file.

### Discoverability & First Impressions
- [x] Add badges to root `README.md` (license, last commit, issues, stars)
- [x] Surface visual proof (screenshot) in root README — extracted from the `ask_gst` demo video, saved to `assets/screenshot_ask_gst.jpg`
- [ ] MANUAL (maintainer only, needs GitHub web UI): set repo Description and Topics — **still pending, only you can do this**

### Community & Trust Signals
- [x] Add `.github/ISSUE_TEMPLATE/` and a PR template — bug report + feature request templates, plus `pull_request_template.md`

### Improvement Suggestions
- [x] Em-dash cleanup in visitor-facing docs: `CONTRIBUTING.md` (root), `src/ask_gst/README.md`, `src/ask_finance/README.md`, `src/ask_dataframe/README.md`, `src/ask_bharat/README.md`, `src/ask_exporter/QUICKSTART.md`, `src/ask_exporter/PLAN.md`
- [ ] Optional/lower priority: same em-dash cleanup in internal notes (`CLAUDE.md`, `Notes.md`, `reports/*.md`) — **explicitly skipped at your direction**
- [x] Extend test coverage beyond the 4 projects that have it — added to `ask_bharat`, `ask_dataframe`, `ask_manahprarupe` (28 new passing tests total)

### Documentation Gaps
- (Covered by `src/utils/README.md` and `src/README.md` items above)

---

## Personal & Sensitive Information — Flagged, No Action Needed

Scanned all tracked filenames for certificate/resume/ID/photo patterns: no matches found. Root `README.md`'s disclaimer line contains the maintainer's own already-published contact address — pre-existing, deliberate, not actionable.

## What's Already Good (carried forward, no action needed)

- Prior upgrade pass (7 Apr 2026) eliminated dead code/duplication/bare-excepts in core projects and added tests + shared `rag_loader.py`
- `LICENSE`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` present and reasonable at root
- `CLAUDE.md` detailed and current
- `ask_exporter` (newest project) is clean and well structured — good template for others
- `ask_yogasutra` benchmark suite, `ask_finance` embedder hierarchy, `ask_faqs_chatbot` vectorizer factory pattern remain solid
