# NOTE: Rasa's dependency tree (TensorFlow, sanic, pydantic, etc.) is notoriously
# strict and can conflict with pandasai/openai in the same environment. If
# `pip install -r requirements.txt` fails to resolve, install the "Rasa server"
# block and the "Flask app" block in two separate virtualenvs (a common Rasa
# deployment pattern: one env runs `rasa train`/`rasa run`, another runs the
# custom action / Flask server).

# Rasa server (rasa train / rasa run) — matches config.yml schema version 3.1
rasa==3.6.21
rasa-sdk==3.6.2

# Flask app + DfEngine/DfLLM query pipeline
flask==2.3.3
requests==2.31.0
beautifulsoup4==4.12.3
pandas==1.5.3
numpy==1.23.5
pandasai==1.5.21
openai==0.28.1
